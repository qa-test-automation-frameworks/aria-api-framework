# Default to deterministic execution. Live API tests share external systems and should
# only be parallelized by an explicit Gradle task or CI matrix that provides isolation.
junit.jupiter.execution.parallel.enabled = false
junit.jupiter.extensions.autodetection.enabled = true
